# Frontend Error Codes

**Version:** 1.0.0  
**Status:** Active  
**Updated:** 2026-01-28  

---

## Overview

Error codes handled and generated by the React frontend.

**Cross-References:**
- [Error Management Overview](../00-overview.md)
- [Shared Error Constants](../shared/01-error-constants.md)
- [Error Boundaries](./02-error-boundaries.md)

---

## Frontend-Specific Codes

### 1xxx - Validation Errors

| Code | Constant | Description |
|------|----------|-------------|
| 1001 | `ERR_VALIDATION_REQUIRED` | Required field is empty |
| 1002 | `ERR_VALIDATION_FORMAT` | Invalid format (email, URL, etc.) |
| 1003 | `ERR_VALIDATION_RANGE` | Value out of allowed range |
| 1004 | `ERR_VALIDATION_LENGTH` | String length violation |
| 1005 | `ERR_VALIDATION_TYPE` | Type mismatch |
| 1010 | `ERR_VALIDATION_FORM` | Generic form validation failure |

### 5xxx - Business Logic (Frontend)

| Code | Constant | Description |
|------|----------|-------------|
| 5100 | `ERR_UI_STATE` | Invalid UI state transition |
| 5101 | `ERR_UI_NAVIGATION` | Navigation blocked (unsaved changes) |
| 5102 | `ERR_UI_SELECTION` | Invalid selection state |
| 5103 | `ERR_UI_CLIPBOARD` | Clipboard operation failed |

---

## Error Code TypeScript Definition

```typescript
// src/lib/errors/codes.ts

export const ERROR_CODES = {
  // Validation (1xxx)
  VALIDATION_REQUIRED: 1001,
  VALIDATION_FORMAT: 1002,
  VALIDATION_RANGE: 1003,
  VALIDATION_LENGTH: 1004,
  VALIDATION_TYPE: 1005,
  VALIDATION_FORM: 1010,
  
  // UI State (5xxx)
  UI_STATE: 5100,
  UI_NAVIGATION: 5101,
  UI_SELECTION: 5102,
  UI_CLIPBOARD: 5103,
} as const;

export type ErrorCode = typeof ERROR_CODES[keyof typeof ERROR_CODES];
```

---

## Usage Examples

### Form Validation

```typescript
import { ERROR_CODES } from '@/lib/errors/codes';
import { AppError } from '@/lib/errors/AppError';

function validateEmail(email: string): void {
  if (!email) {
    throw new AppError(
      ERROR_CODES.VALIDATION_REQUIRED,
      'Email is required',
      { field: 'email' }
    );
  }
  
  if (!email.includes('@')) {
    throw new AppError(
      ERROR_CODES.VALIDATION_FORMAT,
      'Invalid email format',
      { field: 'email', value: email }
    );
  }
}
```

### UI State Error

```typescript
if (hasUnsavedChanges && !confirmDiscard) {
  throw new AppError(
    ERROR_CODES.UI_NAVIGATION,
    'You have unsaved changes',
    { 
      route: targetRoute,
      unsavedFields: ['content', 'title']
    }
  );
}
```

---

## Related Specs

- [Error Boundaries](./02-error-boundaries.md)
- [User Messaging](./03-user-messaging.md)
- [Backend Error Codes](../backend/01-error-codes.md)
