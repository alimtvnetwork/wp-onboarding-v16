# Go Generic Enforce Audit — 2026-02-14

## Completed Phases

### Phase 1: Concrete typed structs ✅
- `apperror.ErrorContext` → `ErrorDiagnostic` struct with 15 typed fields
- `dbops.ContextFields` → `OperationFields` struct with 20+ fields
- `ws.IncomingMessage.Data` → `json.RawMessage` (parse boundary)
- `models.ErrorHistory.Context` / `ErrorLog.Context` → `json.RawMessage`
- `response_types.go`: `ActionResponse.ID` → `string`; paginated responses typed (`[]*middleware.RequestSession`, `[]models.ErrorHistory`, etc.)
- `DirectoryScanResult`, `BulkBootstrapSiteResult` moved to `response_types.go`
- `adapter_plugin.ScanDirectory` returns `*plugin.ScanResult` instead of `any`

### Phase 2: Generic envelope + broadcast ✅
- `envelope.Success[T]`, `Created[T]`, `List[T]`, `ListUnpaginated[T]` — generic with `[T any]`
- `envelope.Deleted` / `Error` use `[]struct{}{}`
- `ws.Broadcast[T]` and `BroadcastWithSession[T]` package-level generic functions
- `respondSuccess[T]`, `respondCreated[T]`, `respondList[T]`, `respondListUnpaginated[T]` in `response.go`
- Deprecated `Hub.Broadcast(string, any)` method retained for backward compat

### Phase 3: WithContext migration ✅
- 39 `WithContext(key, value)` calls migrated to typed setters across 7 files
- Added `WithFile(string)` setter
- Removed deprecated `WithContext(string, any)` compat shim entirely
- Zero `any` parameters remain in the apperror public API

## Remaining Work

### Phase 4: Hub.Broadcast callers + service adapter returns
- ~125 `Hub.Broadcast(eventType, map[string]any{...})` calls → typed event structs + `ws.Broadcast[T](hub, ...)`
- Remove deprecated `Hub.Broadcast(string, any)` method after all callers migrated
- Audit remaining service adapters for `any` return types

### Phase 5: Test files cleanup (optional per GE-5)
- `envelope_test.go` — 3 instances (type assertions)
- `remote_files_test.go` — 1 instance (JSON decode)
