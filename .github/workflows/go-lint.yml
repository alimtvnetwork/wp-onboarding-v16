name: Go Lint & Quality Gates

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - 'licensing/**'
      - 'scripts/**'
  pull_request:
    branches: [main]
    paths:
      - 'backend/**'
      - 'licensing/**'
      - 'scripts/**'

jobs:
  lint-backend:
    name: Backend Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: backend/go.sum

      - name: Go Build Check
        working-directory: backend
        run: go build ./...

      - name: Go Vet
        working-directory: backend
        run: go vet ./...

      - name: Go Tests
        working-directory: backend
        run: go test ./...

      - name: Lint — File Size (backend)
        run: bash scripts/lint-file-size.sh

      - name: Lint — File Size (tools)
        run: bash scripts/lint-file-size.sh --dir tools/consistency-checker

      - name: Lint — Function Size (backend)
        run: bash scripts/lint-func-size.sh

      - name: Lint — Function Size (tools)
        run: bash scripts/lint-func-size.sh --dir tools/consistency-checker

      - name: Lint — Negative Naming
        run: bash scripts/lint-negative.sh

      - name: Lint — Import Grouping
        run: bash scripts/lint-imports.sh

      - name: Lint — Generic Enforce (Go)
        run: bash scripts/lint-ge.sh --go

      - name: Lint — JSON Tags
        run: bash scripts/lint-json-tags.sh

      - name: Lint — Inline If
        run: bash scripts/lint-inline-if.sh

      - name: Validate OpenAPI Spec
        working-directory: backend
        run: |
          if [ -f api/openapi.json ]; then
            npx --yes @redocly/cli lint api/openapi.json --skip-rule no-unused-components
          else
            echo "No OpenAPI spec found, skipping"
          fi

  lint-licensing:
    name: Licensing Server Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: licensing/go.sum

      - name: Go Build Check
        working-directory: licensing
        run: go build ./...

      - name: Go Vet
        working-directory: licensing
        run: go vet ./...

      - name: Go Tests
        working-directory: licensing
        run: go test ./...

      - name: Lint — File Size
        run: bash scripts/lint-file-size.sh --dir licensing

      - name: Lint — Function Size
        run: bash scripts/lint-func-size.sh --dir licensing

      - name: Lint — Negative Naming
        run: bash scripts/lint-negative.sh --dir licensing

      - name: Lint — Import Grouping
        run: bash scripts/lint-imports.sh --dir licensing

      - name: Lint — Generic Enforce (Go)
        run: bash scripts/lint-ge.sh --go

      - name: Lint — JSON Tags
        run: bash scripts/lint-json-tags.sh licensing

      - name: Lint — Inline If
        run: bash scripts/lint-inline-if.sh --dir licensing
